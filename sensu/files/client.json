{%- from "sensu/pillar_map.jinja" import sensu with context %}
{
  "client": {
    {% if sensu.client.add_server_id -%}
    "name": "{{ grains['fqdn'] }}-{{ grains['fqdn'] }}",
    {% else -%}
    "name": {{ grains['fqdn']|json }},
    {% endif -%}
    "address": {{ grains['ipv4'][0]|json }},
    "subscriptions": [ "all" ]
  }
}
