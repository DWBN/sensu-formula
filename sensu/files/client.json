{
  "client": {
    {% if sensu.client.add_server_id -%}
    "name": {{ grains['fqdn']|json }}-{{ grains['fqdn']|json }},
    {% else -%}
    "name": {{ grains['fqdn']|json }},
    {% endif -%}
    "address": {{ grains['ipv4'][1]|json }},
    "subscriptions": [ "all" ]
  }
}
